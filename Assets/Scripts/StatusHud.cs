using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatusHud : MonoBehaviour
{
    /*public Player m_Player;

    public StatusBar experienceBar;

    private float expTimer;

    void Start()
    {
        experienceBar.ChangeMaxValue(Player.MaxExperience);
    }


    void Update()
    {
        if(Time.time > expTimer)
        {
            expTimer = Time.time + 0.1f;
            m_Player.AddExperience(5);
        }

        experienceBar.UpdateInfo(m_Player.Experiencia);
    }*/
}
